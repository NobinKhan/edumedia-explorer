services:
  - type: web
    name: edumedia-author
    env: docker
    plan: free
    autoDeploy: false
    healthCheckPath: /healthz
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        value: sqlite:///./data/edumedia.db
