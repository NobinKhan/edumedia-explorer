# EduMedia Author — project specification

Authoritative hiring-task narrative, data model, API surface, UI expectations, tracker protocol, and definition of done live in **`NewWorkPlan.md`** in the repository root.

This file exists so the repo matches the expected layout from the brief. Implementation status is tracked under **`tracker/`** (especially `current-state.md`, `backlog.md`, and `handoff.md`).
